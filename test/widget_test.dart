import 'package:flutter_test/flutter_test.dart';

void main() {
  // Placeholder — BLE features require a real device and cannot be widget-tested here.
  testWidgets('smoke test placeholder', (tester) async {
    expect(true, isTrue);
  });
}
